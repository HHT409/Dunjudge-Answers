#include <bits/stdc++.h>
#define int long long
using namespace std;

int32_t main() {
    string s;
    cin >> s;
    int sum = 0;
    for (int i = 0; i < 4; i++) {
        if (s[i] == '-') {
            sum--;
        } else if (s[i] == '+') {
            sum++;
        }
    }
    cout << sum;
}
