#include <bits/stdc++.h>
using namespace std;

int main() {
    long long a, n, m;
    cin >> a;
    cin >> n >> m;
    for (int i = n; i <= m; i++) {
        if (i % a == 0) {
            cout << i << '\n';
        }
    }
}
