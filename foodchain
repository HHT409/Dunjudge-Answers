#include <iostream>
#include <bits/stdc++.h>
#include <math.h>
using namespace std;
#define int long long

main() {
    int a, b, c, d, e, n = 1e9 + 9;
    cin >> a >> b >> c >> d >> e;
    cout << ((((((((a % n ) * (b % n)) % n) * (c % n)) % n) * (d % n)) % n) * (e % n)) % n;
}
