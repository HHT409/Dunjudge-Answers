#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n;
    cin >> n;
    cin.ignore();
    string thing;
    getline(cin, thing);
    for (int i = 0; i < n; i++) {
        cout << thing << '\n';
    }
}
