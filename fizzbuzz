#include <bits/stdc++.h>
using namespace std;
#define int long long

main() {
    long long n, m, a, b;
    cin >> n >> m >> a >> b;
    for (long long i = n; i < m; i++) {
        if (i % a == 0 && i % b == 0) {
            cout << "FizzBuzz" << '\n';
        } else if (i % a == 0) {
            cout << "Fizz" << '\n';
        } else if (i % b == 0) {
            cout << "Buzz" << '\n';
        } else {
            cout << i << '\n';
        }
    }
    if (m % a == 0 && m % b == 0) {
        cout << "FizzBuzz";
    } else if (m % a == 0) {
        cout << "Fizz";
    } else if (m % b == 0) {
        cout << "Buzz";
    } else {
        cout << m;
    }
}
