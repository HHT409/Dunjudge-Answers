#include <bits/stdc++.h>
#define int long long
using namespace std;

int32_t main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    int ans = min(b, d) - max(a, c);
    if (ans < 0) {
        cout << "0";
    } else {
        cout << ans;
    }
}
