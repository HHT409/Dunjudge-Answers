#include <bits/stdc++.h>
using namespace std;

int main () {
    long long n;
	cin >> n;
	if (n % 12 == 7) {
		int x = (n % 10) / 2;
		string ans = "";
		if (x == 2) ans = "Wobbit";
		else if (x == 3) ans = "Fibbit";
		else if (x == 4) ans = "Ebbit";
		else if (x == 0) ans = "Mebbit";
		else if (x == 1) ans = "Wabbit";
		cout << ans;
	} else {
		cout << "Sadbit";
	}
}
