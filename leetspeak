#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define int long long

main() {
    int sum = 0;
    string n;
    cin >> n;
    int len = n.size();
    for (int i = 0; i < len; i++) {
        if (isalpha(n[i])) {} else {
            cout << "l33t";
            return 0;
        }
    }
    cout << "English";
}
