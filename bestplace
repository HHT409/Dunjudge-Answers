#include <bits/stdc++.h>
#define int long long
using namespace std;

int x[100005],y[100005];
 
int32_t main(){
    int n;
    cin >> n;
    for (int i = 0; i  <n; i++){
    	cin >> x[i] >> y[i];
	}
	sort(x, x + n);
	sort(y, y + n);
	cout << x[ n/ 2] << " " << y[n / 2];
}
