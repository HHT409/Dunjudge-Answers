#include <bits/stdc++.h>
using namespace std;

int32_t main() {
    long long n, ans;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    sort(arr, arr + n);
    ans = abs(arr[0] - arr[n - 1]);
    cout << ans;
}
