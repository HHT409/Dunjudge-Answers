#include <bits/stdc++.h>
using namespace std;

int32_t main(){
    int n, m;
    cin >> n >> m;
    string str[n];
    for (int i = 0; i < n; i++) {
        cin >> str[i];
    }
    sort(str, str + n);
    for (int i = 0; i < n; i++) {
        cout << str[i];
    }
}
