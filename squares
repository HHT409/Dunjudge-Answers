#include <bits/stdc++.h>
#include <iostream>
using namespace std;
int main() {
    int long long a;
    cin >> a;
    for (int i = 1; i <= a; i++) {
        cout << i*i;
        if (i != a) {
            cout << " ";
        }
    }
}
