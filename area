#include <bits/stdc++.h>
using namespace std;
#define int long long

int32_t main() {
    long long n, sum = 0;
    cin >> n;
    int arr[2];
    for (int i = 0; i < n; i++) {
        cin >> arr[0] >> arr[1];
        sum += arr[0] * arr[1];
    }
    cout << sum;
}
