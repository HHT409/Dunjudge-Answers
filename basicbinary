#include <bits/stdc++.h>
#include <cmath>
using namespace std;

int main() {
    long long sum = 0;
    string n;
    cin >> n;
    for (int i = 0; i < n.size(); i++) {
        if (n[i] == '1') {
            sum += pow(2, n.size() - 1 - i);
        }
    }
    cout << sum;
}
